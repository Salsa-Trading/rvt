
import {FieldDisplay} from './Field';

export function setDefaultWidths(fieldSet: FieldDisplay): FieldDisplay {
  // Recurse over fieldset

  // If we have children, recurse, then set width to sum of children size

  // If we don't have children, set width to some default value if it hasn't been set

  return fieldSet;
}